gpu_access(system_app)

# Allow system_app to write HU mic PCM to the injection FIFO.
allow system_app hu_mic_device:fifo_file { open write getattr };

# Allow system_app to access USB serial devices (ttyUSB*).
allow system_app usb_serial_device:chr_file { read write open ioctl getattr };
